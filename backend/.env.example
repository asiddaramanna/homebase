# Copy to .env and fill in if you need to override the defaults in main.py.
# main.py falls back to the shared classroom RDS + guest read-only creds
# if no env vars are set, so the app will run with no .env present.

PG_HOST=
PG_PORT=
PG_DATABASE=
PG_USER=
PG_PASSWORD=
